import styled from 'styled-components/native';

const Container = styled.View`
  position: absolute;
  bottom: 0;
  padding: 16px;
  width: 100%;
`;
const BottomButtonStyled = {Container};
export default BottomButtonStyled;
