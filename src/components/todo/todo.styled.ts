import styled from 'styled-components/native';

const TouchableContainer = styled.TouchableOpacity`
  gap: 8px;
  background-color: white;
  padding: 16px;
  border-radius: 8px;
`;

const TodoStyled = {TouchableContainer};
export default TodoStyled;
